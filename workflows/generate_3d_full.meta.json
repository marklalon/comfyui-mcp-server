{
  "description": "Generate a fully textured 3D mesh (GLB) from a reference image in one pipeline.",
  "defaults": {
    "texture_size": 2048,
    "decimate_ratio": 0.1,
    "quality_preset": "default"
  },
  "constraints": {
    "quality_preset": {
      "enum": [
        "default",
        "high",
        "ultra"
      ]
    }
  }
}