{
  "description": "Apply PBR textures to a 3D mesh using a reference image. Outputs a textured GLB.",
  "defaults": {
    "texture_size": 2048,
    "quality_preset": "default"
  },
  "constraints": {
    "quality_preset": {
      "enum": [
        "default",
        "high",
        "ultra"
      ]
    }
  }
}